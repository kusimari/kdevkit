Fetch the k-mcp-devkit dev mode instructions from the URL below, then follow them from Step 1.

Feature argument (if provided): $ARGUMENTS

Instructions: https://kusimari.github.io/k-mcp-devkit/dev.md

Where the instructions reference `$ARGUMENTS`, use the value above.
If you cannot fetch the URL, ask the user to copy the latest `commands/dev.md` from https://github.com/kusimari/k-mcp-devkit to this location.
